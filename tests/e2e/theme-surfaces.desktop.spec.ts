import{expect,test}from'@playwright/test';
test('search, login and AIMS landing use current supported surfaces',async({page})=>{await page.addInitScript(()=>{if(!localStorage.getItem('kcs-auth'))localStorage.setItem('kcs-auth','in');localStorage.setItem('kcs-role','administrator');localStorage.setItem('kcs-theme','kcs-forest-gold')});await page.goto('/dashboard');await expect(page.locator('.global-search kbd')).toBeHidden();const search=await page.locator('.global-search').boundingBox(),bell=await page.locator('.top-notifications').boundingBox();expect(search&&bell&&bell.x-(search.x+search.width)).toBeLessThan(30);await page.goto('/');await expect(page.locator('html')).toHaveAttribute('data-theme','kcs-forest-gold');await expect(page.locator('.aims-public-page')).toHaveAttribute('data-theme','aimsAzureGlass');expect(await page.locator('.aims-glossy-button').first().evaluate(element=>getComputedStyle(element).backgroundImage)).toContain('linear-gradient');await page.evaluate(()=>localStorage.setItem('kcs-auth','out'));await page.goto('/login');await expect(page.locator('.login-page__media')).toBeHidden();expect(await page.locator('.auth-panel').evaluate(element=>getComputedStyle(element).backgroundColor)).toBe('rgb(255, 255, 255)');expect(await page.locator('.login-card').evaluate(element=>({background:getComputedStyle(element).backgroundColor,border:getComputedStyle(element).borderTopWidth,shadow:getComputedStyle(element).boxShadow}))).toEqual({background:'rgba(0, 0, 0, 0)',border:'0px',shadow:'none'})});
