import {expect,test} from '@playwright/test';
test('AIMS Azure Glass public page reflows and switches language',async({page})=>{await page.addInitScript(()=>{localStorage.setItem('kcs-auth','out');localStorage.setItem('kcs-language','en')});await page.goto('/');await page.getByRole('button',{name:'Open navigation'}).click();await page.getByLabel('Language').selectOption('nl');await expect(page.getByRole('heading',{level:1})).toContainText('Beheer elk middel');await expect(page.locator('[data-device="mobile"]')).toBeVisible();await expect(page.locator('[data-device="desktop"]')).toBeHidden();expect(await page.evaluate(()=>document.documentElement.scrollWidth)).toBeLessThanOrEqual(390);await page.screenshot({path:'docs/design/aims-azure-glass-public-mobile.png',fullPage:true})});
