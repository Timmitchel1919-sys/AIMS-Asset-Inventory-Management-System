import {expect,test} from '@playwright/test';
test('AIMS Midnight public page reflows and switches language',async({page})=>{await page.addInitScript(()=>{localStorage.setItem('kcs-auth','out');localStorage.setItem('kcs-language','en');localStorage.setItem('authenticatedThemePreference','aimsMidnight')});await page.goto('/');await expect(page.locator('html')).toHaveAttribute('data-theme','aims-midnight');await page.getByLabel('Language').selectOption('nl');await expect(page.getByRole('heading',{level:1})).toContainText('Beheer elk middel');await expect(page.locator('.aims-hero-device-showcase .aims-device-stage')).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth)).toBeLessThanOrEqual(390);await page.screenshot({path:'docs/design/aims-midnight-public-mobile.png',fullPage:true})});
