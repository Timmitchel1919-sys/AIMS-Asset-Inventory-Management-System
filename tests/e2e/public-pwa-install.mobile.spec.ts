import{expect,test}from'@playwright/test';
test('mobile landing download invokes the browser install prompt directly',async({page})=>{await page.addInitScript(()=>localStorage.setItem('kcs-auth','out'));await page.goto('/');await page.evaluate(()=>{const event=new Event('beforeinstallprompt');Object.assign(event,{prompt:async()=>{document.documentElement.dataset.installPrompt='opened'},userChoice:Promise.resolve({outcome:'accepted'})});dispatchEvent(event)});const button=page.getByRole('button',{name:'Download App'}).first();await expect(button).toBeVisible();await button.click();await expect(page.locator('html')).toHaveAttribute('data-install-prompt','opened');await expect(page.getByRole('dialog')).toHaveCount(0);expect(await page.evaluate(()=>document.documentElement.scrollWidth)).toBeLessThanOrEqual(390)});
