import {expect,test} from '@playwright/test';
test('AIMS Midnight public page renders its device showcase without overflow',async({page})=>{const errors:string[]=[];page.on('console',message=>{if(message.type()==='error')errors.push(message.text())});await page.addInitScript(()=>{localStorage.setItem('kcs-auth','out');localStorage.setItem('kcs-language','en');localStorage.setItem('authenticatedThemePreference','aimsMidnight')});await page.goto('/');await expect(page.locator('html')).toHaveAttribute('data-theme','aims-midnight');await expect(page.getByRole('heading',{level:1})).toContainText('Manage every asset');await expect(page.locator('.aims-orbital-hero')).toBeVisible();await expect(page.locator('.aims-showcase-section .aims-device-stage')).toBeVisible();await expect(page.locator('.aims-feature-grid article')).toHaveCount(8);expect(await page.evaluate(()=>document.documentElement.scrollWidth)).toBeLessThanOrEqual(1366);await page.screenshot({path:'docs/design/aims-midnight-public.png',fullPage:true});await page.locator('.aims-language select').selectOption('nl');await expect(page.getByRole('heading',{level:1})).toContainText('Beheer elk middel');await page.getByLabel('Public navigation').getByRole('link',{name:'Functies',exact:true}).click();await expect(page.locator('#features')).toBeInViewport();expect(errors).toEqual([])});
