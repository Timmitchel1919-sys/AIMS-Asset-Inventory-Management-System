import{expect,test}from'@playwright/test';
test('global search remains inside the mobile viewport',async({page})=>{await page.goto('/dashboard');const search=page.getByRole('combobox',{name:/global search/i});await search.fill('kcsmd');await expect(page.getByRole('option',{name:/Dell Latitude 5420/})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth)).toBeLessThanOrEqual(390);await page.screenshot({path:'docs/design/global-search-mobile.png'});await page.getByRole('button',{name:/clear search/i}).click();await expect(page.getByRole('listbox')).not.toBeVisible()});
