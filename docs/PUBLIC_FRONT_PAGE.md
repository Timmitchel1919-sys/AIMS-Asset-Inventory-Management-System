# Public Front Page

Public support links reuse the canonical `/support` route. The login ICT Support dialog reuses `/support#ticket-system` as a clearly disclosed presentation placeholder until a real ticket route and backend exist. Approved contact data is centralized in `src/config/ictSupport.ts` and must not be duplicated in landing-page content.
