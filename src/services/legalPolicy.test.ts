import {describe,expect,it} from 'vitest';
import {acknowledgementId,canTransitionLegalStatus,recordAcknowledgement,validatePolicyVersion} from './legalPolicy';
describe('legal policy safeguards',()=>{it('allows only approved workflow transitions',()=>{expect(canTransitionLegalStatus('draft','pending_approval')).toBe(true);expect(canTransitionLegalStatus('draft','published')).toBe(false);expect(canTransitionLegalStatus('archived','draft')).toBe(false)});it('validates versions',()=>{expect(validatePolicyVersion('1.0')).toBe(true);expect(validatePolicyVersion('v1')).toBe(false)});it('creates identity-bound acknowledgement ids',()=>{expect(acknowledgementId('u1','terms','1.0')).toBe('u1_terms_1.0')});it('records only the supplied authenticated identity',()=>{const storage=new Map<string,string>();const fake={setItem:(key:string,value:string)=>storage.set(key,value)} as unknown as Storage;const result=recordAcknowledgement(fake,'u1','privacy','1.0','settings');expect(result.userId).toBe('u1');expect(storage.has('kcs-policy-ack:u1_privacy_1.0')).toBe(true)})});
